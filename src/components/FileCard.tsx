// Re-export of the resources feature FileCard so existing import sites
// (e.g. the Profile page) keep working.
export { FileCard, type FileCardProps } from "@/features/resources/components/FileCard";
export { default } from "@/features/resources/components/FileCard";
